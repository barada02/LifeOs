# Life OS MCP Server — Dependencies
mcp


# MongoDB async driver
motor
pymongo
dnspython

# Data validation
pydantic

# Environment variables
python-dotenv

# NOTE: Do NOT add `bson` here. pymongo bundles its own bson module.
# Installing the standalone `bson` PyPI package will shadow it and cause ImportError.

# Async HTTP (required by fastmcp SSE transport)
httpx
anyio