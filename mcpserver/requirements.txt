# Life OS MCP Server — Dependencies

# MCP server framework
fastmcp>=0.1.0

# MongoDB async driver
motor>=3.0.0
pymongo>=4.0.0
dnspython>=2.0.0

# Data validation
pydantic>=2.0.0

# Environment variables
python-dotenv>=1.0.0

# BSON (ObjectId handling — bundled with pymongo, listed explicitly for clarity)
# pymongo already includes bson

# Async HTTP (required by fastmcp SSE transport)
httpx>=0.25.0
anyio>=4.0.0
